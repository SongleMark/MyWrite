
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_LEN 10

enum RETURN_TYPE {
    RETURN_TRUE = 0,
    RETURN_FALSE
};

typedef struct ArrayInfo {
    int length;
    int array[MAX_LEN];
} ARRAYINFO;

void AddDataToArray(int array[], int index, int value) {
    if(index >= length)
}

int main(int argc, char *argv[]) {
    return 0;
}
