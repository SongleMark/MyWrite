#include <iostream>
#include <string>
#include <vector>
#include <cstdio>
#include <cstring>
using namespace std;

int main()
{
	/*string str;
	getline(cin, str);
	cout << "str = " << str << endl;
	char data[32];
	strcpy(data, str.c_str());
	cout << "data = " << data << endl;*/
	char st[32];
	cin.get(st, 32);
	cout << "st = " << st << endl;
	//char data[32];
	//cin >> str;
	//fgets(data, 30, stdin);
	//cout << "data = " << data << endl;
	return 0;
}
